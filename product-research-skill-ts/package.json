{
  "name": "product-research-skill-ts",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript implementation of product research skill",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/cli.js",
    "test": "npm run build && node --test dist/test/*.test.js"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
